server:
 port: 8080
 servlet:
  context-path: /bootcamp
  
  
spring:
 datasource:
  url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/bootcamp
  username: root
  password: 
  driver-class-name: com.mysql.jdbc.Driver
 jpa:
  show-sql: true
  database-platform: org.hibernate.dialect.MySQLDialect
  generate-ddl: false
  hibernate:
    ddl-auto: none
      
application:
 description: Projeto criado para o bootcamp DIO - Santander
 version: 1.0